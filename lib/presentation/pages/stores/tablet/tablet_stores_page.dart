import 'package:flutter/material.dart';

class TabletStoresPage extends StatelessWidget {
  const TabletStoresPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Tablet Stores Page")));
  }
}
